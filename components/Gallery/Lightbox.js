function photoPath(root, filename) {
  return `${root}/${filename}.webp`;
}

// TODO figure out how to add an accessible modal
export default function Lightbox({ photo }) {
  return <div>Lightbox</div>;
}
